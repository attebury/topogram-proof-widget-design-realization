shape shape_input_list_collections {
  name "List Collections Input"
  description "Input for listing collections"

  fields {
    after text optional
    limit integer optional default 25
  }

  status active
}
