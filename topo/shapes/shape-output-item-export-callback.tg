shape shape_output_item_export_callback {
  name "Item Export Callback"
  description "Outbound webhook payload sent when a item export completes"

  fields {
    job_id uuid required
    status export_job_status required
    download_url text optional
    error_message text optional
    completed_at datetime optional
  }

  status active
}
