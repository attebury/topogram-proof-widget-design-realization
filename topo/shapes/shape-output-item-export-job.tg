shape shape_output_item_export_job {
  name "Item Export Job"
  description "Accepted job payload for long-running item exports"

  fields {
    job_id uuid required
    status string required default accepted
    status_url text required
    submitted_at datetime required
  }

  status active
}
